function compareLocations(data, currentLocation, salary) {
    // compare percentage score of current combination with all other cities
    
    var topAlternativeCities = [];
    
    return topAlternativeCities;
}